/**
 * Phase 08C: response controls are driven by evaluation.editable from the API.
 * Kept for backwards-compatible imports; prefer hasEvaluationResponseControls(evaluation).
 */
export { hasEvaluationResponseControls } from "@/lib/performance/response-workspace";
